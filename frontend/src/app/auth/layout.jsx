/**
 * Layout pour les pages auth (login, register, etc.)
 * Supprime le Header et Footer du layout principal via un layout segment.
 */
export default function AuthLayout({ children }) {
  return <>{children}</>;
}
